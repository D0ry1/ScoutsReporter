<Window x:Class="ScoutsReporter.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Password-Protect Export"
        SizeToContent="Height" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="20" TextElement.Foreground="{DynamicResource PrimaryTextBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Enter a password to protect the exported file:"
                   Margin="0,0,0,8" TextWrapping="Wrap" />

        <Border Grid.Row="1" Background="{DynamicResource DialogInfoBoxBackgroundBrush}"
                BorderBrush="{DynamicResource DialogInfoBoxBorderBrush}" BorderThickness="1"
                CornerRadius="4" Padding="10" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Password must contain:" FontWeight="SemiBold" FontSize="12"
                           Margin="0,0,0,4" />
                <TextBlock x:Name="RuleLength" Text="&#x2022; At least 8 characters" FontSize="11.5"
                           Foreground="{DynamicResource PlaceholderTextBrush}" />
                <TextBlock x:Name="RuleUpper" Text="&#x2022; An uppercase letter (A-Z)" FontSize="11.5"
                           Foreground="{DynamicResource PlaceholderTextBrush}" />
                <TextBlock x:Name="RuleLower" Text="&#x2022; A lowercase letter (a-z)" FontSize="11.5"
                           Foreground="{DynamicResource PlaceholderTextBrush}" />
                <TextBlock x:Name="RuleDigit" Text="&#x2022; A number (0-9)" FontSize="11.5"
                           Foreground="{DynamicResource PlaceholderTextBrush}" />
            </StackPanel>
        </Border>

        <PasswordBox x:Name="PasswordInput" Grid.Row="2"
                     Margin="0,0,0,8" Padding="4"
                     PasswordChanged="OnPasswordChanged" />

        <PasswordBox x:Name="ConfirmInput" Grid.Row="3"
                     Margin="0,0,0,8" Padding="4"
                     PasswordChanged="OnPasswordChanged" />

        <TextBlock x:Name="ErrorText" Grid.Row="4"
                   Foreground="{DynamicResource ErrorTextBrush}" FontSize="12"
                   Margin="0,0,0,8" TextWrapping="Wrap" />

        <StackPanel Grid.Row="5" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="0,0,8,0"
                    x:Name="OkButton" Click="OnOkClicked"
                    IsDefault="True" />
            <Button Content="Cancel" Width="80"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
